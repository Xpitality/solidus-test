window.addEventListener("load", () => {
  handleCartModal();
  handleHeader();
  handleSwiper();
  handleProfile();
  handleToggleForm();
  handlePriceSlider();
  handleFilterOptions();
  handleSeeMoreOptions();
});
