<% if @page %>

  <% if %w(il-locale chi-siamo spedizione).include?(params['id']) %>
    <%= render partial: "spree/pages/show/page_#{params['id']}", locals: { page: @page } %>
  <% else %>
    <%= render partial: "spree/pages/show/page_generic", locals: { page: @page } %>
  <% end %>

<% end %>